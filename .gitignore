# Сохранённое состояние песочницы (не коммитим — содержит ключи агентов)
data/

# Зависимости (их нет, но на будущее)
node_modules/

# Vercel (линк проекта и кэш сборки)
.vercel/

# Системное
.DS_Store
*.log
